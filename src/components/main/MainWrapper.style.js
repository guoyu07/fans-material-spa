// @flow

import withStyles from 'material-ui/styles/withStyles';

export default withStyles(() => ({
  root: {
    width: '100%',
    boxSizing: 'border-box',
  },
  xsRoot: {
    paddingTop: 56,
    paddingBottom: 56,
    minHeight: '100vh',
  }
}));
